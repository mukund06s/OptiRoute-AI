// Database seed script — implementation in Phase 1 (DB setup) per PRD Section 5

async function main(): Promise<void> {
  // Seed data will be added in a later phase
}

main()
  .catch((error: unknown) => {
    console.error(error);
    process.exit(1);
  });
